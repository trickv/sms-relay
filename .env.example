# SMS to Mastodon Relay Configuration

# Phone number to filter (10 digits, no special characters)
SOURCE_PHONE_NUMBER=7152009057

# Mastodon instance URL (include https://)
MASTODON_INSTANCE_URL=https://mastodon.social

# Mastodon access token (create an app in your instance settings)
# Go to: Settings > Development > New Application
# Scopes needed: write:statuses
MASTODON_ACCESS_TOKEN=your_access_token_here

# Polling interval in seconds (default: 60)
POLL_INTERVAL_SECONDS=60

# File to track processed messages (default: .processed_messages.txt)
STATE_FILE=.processed_messages.txt
